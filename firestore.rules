rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // L'API utilise firebase-admin (bypass des règles).
    // Ces règles bloquent l'accès client direct par défaut.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
